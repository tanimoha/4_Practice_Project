name: Deployment Exercise 2
on: push
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
        - name: get code
          uses: actions/checkout@v4
        - name: Install Dependencies
          uses: npm ci
        - name: Lint
          uses: npm run lint
  test:
    runs-on: ubuntu-latest
    steps:
        - name: get code
          uses: actions/checkout@v4
        - name: Install Dependencies
          uses: npm ci
        - name: Test Code
          uses: npm run test
  deploy:
    runs-on: ubuntu-latest
    steps:
        - name: get code
          uses: actions/checkout@v4
        - name: Install Dependencies
          uses: npm ci
        - name: Build code
          uses: npm run build
        - name: Deploy code
          uses: echo "Deploying..."
          